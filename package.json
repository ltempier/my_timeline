{
  "name": "my_timeline",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "css": "cleancss -o dist/my-timelyne.min.css src/style.css",
    "js": "babel src/index.js --presets es2015 --out-file dist/MyTimeline.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "npm run js && npm run css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jquery": "^2.2.4",
    "moment": "^2.18.1"
  },
  "devDependencies": {
    "babel-cli": "^6.0.0",
    "babel-preset-es2015": "^6.24.1",
    "clean-css-cli": "^4.1.6"
  }
}
